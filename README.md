# webflux-tracing-issue

```shell
./gradlew bootRun
```

```shell
curl http://localhost:8080/hello
curl http://localhost:8080/hello
curl http://localhost:8080/hello
curl http://localhost:8080/hello
...
```

The traceId is always the same.
